---
import Base from '../layouts/Base.astro';
---

<Base title="Pan — coming soon.">
  <main
    style="
      min-height: 100svh;
      display: grid;
      place-items: center;
      font-family: var(--font-display);
      font-size: var(--text-3xl);
      color: var(--fg-muted);
      letter-spacing: var(--tracking-tight);
    "
  >
    Pan &mdash; coming soon.
  </main>
</Base>
